# Git 命名規則ガイド

> ブランチ名やコミットメッセージの書き方を統一することで、
> チーム全員が Git Graph を見たときに「誰が・何を・なぜ」やったか一目で分かるようになります。

---

## 1. ブランチの命名規則

### 基本の形

```
種類/作業内容
```

### 種類の一覧

| 種類 | 意味 | 例 |
|------|------|----|
| `feature/` | 新しい機能を追加する | `feature/top-page` |
| `fix/` | バグや間違いを直す | `fix/login-error` |
| `docs/` | ドキュメント・説明文を編集する | `docs/readme-update` |
| `style/` | デザインや見た目を変える | `style/header-color` |
| `refactor/` | 動きはそのままでコードを整理する | `refactor/map-component` |

### 命名のルール

- **英語の小文字** で書く（大文字・日本語はNG）
- 単語の区切りは **ハイフン（-）** を使う（アンダースコアはNG）
- 作業内容は **短く・具体的に** 書く

| OK | NG | 理由 |
|----|-----|------|
| `feature/top-page` | `feature/TopPage` | 大文字はNG |
| `fix/login-error` | `fix/ログインエラー` | 日本語はNG |
| `feature/map-search` | `feature/new_function` | 内容が不明確 |
| `fix/button-color` | `fix/fix` | 短すぎて意味不明 |

### Git Graph での見え方

ブランチ名が正しくついていると、Git Graph でグラフを見たときに
それぞれの作業が何をしているか **一目で分かる** ようになります。

---

## 2. コミットメッセージの命名規則

### 基本の形

```
種類: 何をしたか
```

### 種類の一覧

| 種類 | 意味 | 例 |
|------|------|----|
| `feat` | 新しい機能を追加した | `feat: トップページを追加` |
| `fix` | バグを直した | `fix: ログインできない問題を修正` |
| `docs` | ドキュメントを更新した | `docs: READMEを更新` |
| `style` | デザインを変更した | `style: ヘッダーの色を変更` |
| `refactor` | コードを整理した | `refactor: マップ処理を整理` |
| `remove` | ファイルや機能を削除した | `remove: 不要なファイルを削除` |

### コミットメッセージのルール

- **何をしたか** が一文で分かるように書く
- 「〜を追加」「〜を修正」など **動詞で終わらせる**
- 長すぎず **50文字以内** を目安にする
- 日本語でも英語でもOKだが **チーム内で統一する**

| OK | NG | 理由 |
|----|-----|------|
| `feat: トップページを追加` | `commit` | 内容が何も分からない |
| `fix: ボタンの色を修正` | `いろいろ直した` | 種類がなく内容も曖昧 |
| `docs: READMEを更新` | `READMEちょっと変えた` | 種類がない |
| `fix: ログインエラーを修正` | `fix: ログインのバグを発見して調査して修正しました` | 長すぎる |

### Git Graph での見え方

コミットメッセージが正しくついていると、Git Graph のグラフ上で
各コミットの内容が **一行で確認できる** ようになります。
レビューやコンフリクト解消のときに役立ちます。

---

## 3. チームで守るルールまとめ

| 項目 | ルール |
|------|--------|
| ブランチ名 | `種類/作業内容`（英語・小文字・ハイフン区切り） |
| コミットメッセージ | `種類: 何をしたか`（50文字以内） |
| `main` への直接 push | **禁止**（必ずブランチを作ってプルリクエスト） |
| 1コミットの単位 | **1つの作業につき1コミット**（複数の変更をまとめない） |
| 作業前 | 必ず `git pull origin main` で最新状態にする |
