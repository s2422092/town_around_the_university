# GitHub 使い方ガイド（Git Graph 版）

> VS Code の拡張機能 **「Git Graph」** を使って、ブランチの作成・マージ・コンフリクト解消をする方法を説明します。

---

## 準備：Git Graph をインストールする

1. VS Code を開く
2. 左サイドバーの拡張機能アイコン（四角が4つ並んだマーク）をクリック
3. 検索欄に `Git Graph` と入力
4. 「Git Graph」（作者: mhutchie）をインストール

インストールが完了すると、VS Code 下部のステータスバーに `Git Graph` ボタンが表示されます。

---

## Git Graph の開き方

- VS Code 下部のステータスバーにある **「Git Graph」** をクリック
- または、コマンドパレット（`Cmd+Shift+P`）で `Git Graph: View Git Graph` と入力

コミット履歴やブランチが **グラフ形式** で一覧表示されます。

---

## 1. ブランチを作る

### コマンドで作る場合

```bash
# 最新の状態にする
git pull origin main

# 新しいブランチを作って移動
git checkout -b feature/自分の作業名
# 例: git checkout -b feature/top-page
```

### Git Graph から作る場合

1. Git Graph を開く
2. `main` のコミット行を **右クリック**
3. 「Create Branch...」をクリック
4. ブランチ名を入力して「Create Branch」

作成したブランチがグラフ上に表示されます。

### ブランチ名のつけ方
| 作業の種類 | 名前の例 |
|-----------|---------|
| 新しい機能を作る | `feature/機能名` |
| バグを直す | `fix/バグの内容` |
| 文章・ドキュメントを直す | `docs/内容` |

---

## 2. 作業してコミット・プッシュする

```bash
# 変更したファイルを登録
git add .

# 何をしたかメモをつけて保存（コミット）
git commit -m "トップページを作成した"

# GitHubに送る（プッシュ）
git push origin feature/top-page
```

Git Graph でコミットが積み上がっていく様子が視覚的に確認できます。

---

## 3. マージする（自分の変更を main に取り込む）

### Git Graph からマージする

1. Git Graph を開く
2. マージしたいブランチ（例: `feature/top-page`）のコミットを **右クリック**
3. 「Merge into current branch...」をクリック
4. 確認画面が出るので「Yes, merge」

マージ後、グラフ上でブランチが `main` に合流しているのが確認できます。

### GitHubでプルリクエストを作る場合

1. GitHubでリポジトリを開く
2. 「Compare & pull request」ボタンをクリック
3. `base: main` ← `compare: 自分のブランチ名` を確認
4. タイトルと説明を書いて「Create pull request」
5. チームメンバーに確認してもらい「Merge pull request」

---

## 4. コンフリクトが起きたときの対処法

### コンフリクトって何？

2人が同じファイルの同じ行を別々に編集すると、
「どちらの内容を使えばいいか分からない！」となります。
これが **コンフリクト（競合）** です。

### Git Graph でコンフリクトを確認する

Git Graph でコンフリクト中のコミットには **赤いマーク** が表示されます。
クリックするとどのファイルが競合しているか確認できます。

### 解消の手順

**ステップ1：コンフリクトしているファイルを確認する**

```bash
git status
# "both modified: ファイル名" と表示されているものがコンフリクト中
```

**ステップ2：ファイルを編集して解消する**

VS Code でコンフリクト中のファイルを開くと、
「現在の変更を取り込む」「受信した変更を取り込む」「両方を取り込む」
というボタンが表示されるので、残したい内容を選ぶ。

手動で直す場合は `<<<<<<<`, `=======`, `>>>>>>>` の行ごと削除して残したい内容だけにする。

修正前：
```
<<<<<<< HEAD
トップページへようこそ
=======
大学周辺マップへようこそ
>>>>>>> feature/other-branch
```

修正後：
```
大学周辺マップへようこそ
```

**ステップ3：解消したらコミットして完了**

```bash
git add .
git commit -m "fix: コンフリクトを解消"
git push origin 自分のブランチ名
```

Git Graph でグラフが正常に繋がっていれば解消完了です。

---

## 困ったときのコマンド早見表

| やりたいこと | コマンド |
|-------------|---------|
| 今の状態を確認 | `git status` |
| ブランチ一覧を見る | `git branch` |
| 最新状態を取得 | `git pull origin main` |
| 変更を登録 | `git add .` |
| コミット | `git commit -m "メッセージ"` |
| GitHubに送る | `git push origin ブランチ名` |
| 作業を途中で退避 | `git stash` |
| 退避した作業を戻す | `git stash pop` |
